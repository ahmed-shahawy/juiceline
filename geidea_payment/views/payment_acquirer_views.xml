<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Geidea Payment Provider Form View -->
        <record id="payment_provider_form_inherit_geidea" model="ir.ui.view">
            <field name="name">payment.provider.form.inherit.geidea</field>
            <field name="model">payment.provider</field>
            <field name="inherit_id" ref="payment.payment_provider_form"/>
            <field name="arch" type="xml">
                <xpath expr="//group[@name='provider_credentials']" position="after">
                    <group attrs="{'invisible': [('code', '!=', 'geidea')]}" string="Geidea Credentials">
                        <field name="geidea_merchant_id" required="True" attrs="{'required': [('code', '=', 'geidea')]}"/>
                        <field name="geidea_api_key" required="True" attrs="{'required': [('code', '=', 'geidea')]}" password="True"/>
                        <field name="geidea_test_mode"/>
                    </group>
                </xpath>
            </field>
        </record>
    </data>
</odoo>